server:
  port: 8081
  servlet.context-path: /payment
spring:
  h2:
    console.enabled: true
  datasource:
    url: jdbc:h2:file:/Users/vishrutmajmudar/payment
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  application:
    name: payment-service